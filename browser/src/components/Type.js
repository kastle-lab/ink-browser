import React from 'react'

function Type({ data, setData }) {
  
  return (
    <div className='type'>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
      <p>Data from node {data && data.data.label}</p>
    </div>
  )
}

export default Type